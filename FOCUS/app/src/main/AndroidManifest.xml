<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="abdelsattar.com.focus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activties.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activties.ToDoList"
            android:label="@string/title_activity_to_do_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activties.subtasksDetails" />
        <activity android:name=".Activties.TopTasks" />
        <activity android:name=".Activties.Contact" />
        <activity android:name=".Activties.Thankful"></activity>
    </application>

</manifest>